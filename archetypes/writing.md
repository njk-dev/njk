---
title: "{{ replace .File.ContentBaseName "-" " " | title }}"
date: {{ .Date }}
draft: true
genre: essay
# occasion: appended to the dateline after a dim dot — a feast, an anniversary, or nothing
# folio: assigned by hand at publication — next in the ledger (fol. 03+), never reused
#
# unlisted leaves — in the volume, not in the count:
# unlisted: true
# build:
#   list: never
---
